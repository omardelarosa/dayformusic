var jade = require('jade');
function template(locals) {
var buf = [];
var jade_mixins = {};

buf.push("<div class=\"m\">untitled</div>");;return buf.join("");
}

/**
 * Export module
 */

module.exports = template;